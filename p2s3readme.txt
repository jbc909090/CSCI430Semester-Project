Files in nf6610wt in the studnet work folder in course files
under the folder Project2Stage3
download all the files and make sure you have have some accesible x11 server
then in the folder with all the files run
javac WareContext.java
then run
java WareContext
this will take a bit but then the GUI will appear, every user input is done via GUI
every output is via text in the command line so check there for things such as the lsit of clients with outstanding balances